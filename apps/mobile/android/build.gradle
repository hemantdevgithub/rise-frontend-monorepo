plugins {
    id("com.android.application") // No need to specify version again here
    id("org.jetbrains.kotlin.android")
    id("com.facebook.react")
}

android {
    namespace "com.rise.mobile"
    compileSdk = 34

    defaultConfig {
        applicationId "com.rise.mobile"
        minSdk = 24
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildToolsVersion = "34.0.0"
    ndkVersion = "25.2.9519653"
}

repositories {
    google()
    mavenCentral()
}
